# Server Configuration
NODE_ENV=development
PORT=3000

# Database
# Option 1: Use DATABASE_URL (Railway's pre-constructed connection string)
# DATABASE_URL=postgresql://user:pass@host:port/dbname

# Option 2: Use individual PostgreSQL variables (will be constructed automatically)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=fit_gpt_dev

# Redis Cache (Optional - improves exercise search performance)
# Leave commented out to run without cache
# REDIS_URL=redis://localhost:6379

# JWT Configuration
# SECURITY: Generate a cryptographically secure secret with: openssl rand -base64 32
# REQUIRED in production - application will fail to start if not set
# Use the default below only for local development
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000

# API Keys
ANTHROPIC_API_KEY=your-api-key-here
OPENAI_API_KEY=your-openai-api-key-here
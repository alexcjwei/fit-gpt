# Server Configuration
NODE_ENV=development
PORT=3000

# Database
# Option 1: Use MONGODB_URI (backwards compatible)
MONGODB_URI=mongodb://localhost:27017/fit-gpt

# Option 2: Use Railway's MONGO_URL (pre-constructed connection string)
# MONGO_URL=mongodb://user:pass@host:port/dbname

# Option 3: Use Railway's individual MongoDB variables (will be constructed automatically)
# MONGOHOST=your-railway-host
# MONGOPORT=27017
# MONGOUSER=your-railway-user
# MONGOPASSWORD=your-railway-password

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000

# API Keys
ANTHROPIC_API_KEY=your-api-key-here
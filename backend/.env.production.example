# Server Configuration
NODE_ENV=production
PORT=3000

# Database Configuration
# Priority order:
# 1. MONGO_URL (Railway's pre-constructed URL)
# 2. MONGOHOST, MONGOPORT, MONGOUSER, MONGOPASSWORD (Railway's individual variables)
# 3. MONGODB_URI (MongoDB Atlas or custom connection string)

# Option 1: Railway's MONGO_URL (recommended for Railway deployments)
# Railway provides this automatically when you add MongoDB
# MONGO_URL=mongodb://user:pass@host:port/dbname

# Option 2: Railway's individual MongoDB variables (also provided automatically)
# MONGOHOST=your-railway-mongodb-host
# MONGOPORT=27017
# MONGOUSER=your-railway-user
# MONGOPASSWORD=your-railway-password

# Option 3: MongoDB Atlas or custom connection string (backwards compatible)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/fit-gpt?retryWrites=true&w=majority

# JWT Configuration
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-production-secret-key-here
JWT_EXPIRES_IN=7d

# CORS
# Comma-separated list of allowed origins
# Use * for development, specific domains for production
CORS_ORIGIN=https://your-frontend-domain.com,exp://

# API Keys
ANTHROPIC_API_KEY=your-anthropic-api-key-here
